<!DOCTYPE html>
<html lang ="en">
<meta charset ="UTF-8">
<head>
 <title>Ticket App</title>
 <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class ="max-w-[1440px]">
<header class="bg-white shadow p-4 flex lg:flex-row md:flex-row flex-row justify-between lg:items-center lg:w-full lg:px-[5em] md:px-[2em] sticky lg:top-0  z-50 gap-6">
  <a href="/" class="text-xl font-bold text-indigo-600">🎟️ TicketApp</a>

  {% if isAuthenticated and user %}
    <div class="flex items-center gap-4">
      <div class="flex items-center gap-2">
        <div class="w-8 h-8 rounded-full bg-indigo-500 text-white flex items-center justify-center uppercase">
          {{ user.email[0] }}
        </div>
        <span class="text-sm text-gray-700">{{ user.email }}</span>
      </div>
      {% if location != '/dashboard' %}
        <form method="post" action="/logout">
          <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600">
            Logout
          </button>
        </form>
      {% endif %}
    </div>
  {% else %}
    <div class="flex gap-2">
      <a href="/?page=login" class="text-indigo-600 hover:underline">Login</a>
      <a href="/?page=signup" class="text-purple-600 hover:underline">Sign Up</a>
    </div>
  {% endif %}
</header>
</body>

</html>
